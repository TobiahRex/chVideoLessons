/*
import I18n from 'i18n-react';

export default kelvin => {
const celcius = kelvin - 273.15;
const farenheit = (celcius * 1.80000) + 32;

if (I18n.translate('tempIndicator') === 'F') return Math.round(farenheit);
return Math.round(celcius);
};
*/
